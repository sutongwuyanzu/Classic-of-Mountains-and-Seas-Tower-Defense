# Steam Release QA Matrix

最后更新：2026-09-19 19:43 +08:00
状态定义：`通过` 为已在本轮实际验证；`待测` 为尚未执行；`阻塞` 为缺少外部前置条件。

| 区域 | 用例 | 当前状态 | 验证方式 / 证据 |
| --- | --- | --- | --- |
| 构建 | 免安装 Windows 包可生成 | 通过 | `npm run dist:unpacked`。 |
| 构建 | 发行 EXE 可启动 | 通过 | 隔离 `user-data-dir` 启动 `ShanHaiDefense.exe`，5 秒存活且窗口标题正确。 |
| 构建 | NSIS 安装、已安装 EXE 启动 | 通过 | 静默安装退出码 0；`G:\Codex files\steam-s01-nsis-20260919-0055\install\山海异兽志.exe` 启动后窗口标题正确。 |
| 构建 | 临时便携 EXE 可启动 | 通过 | `dist:portable` 产物启动并派生窗口标题为“山海异兽志”的 Electron 进程。 |
| 构建 | 发行清单标记源码状态 | 通过 | `npm run manifest:steam` 实测输出当前提交、`dirty: true`、Electron `38.8.6`、electron-builder `26.15.3` 及产物 SHA-256；dirty 产物不作为干净发布候选。 |
| 构建 | 包中不带素材源文件和提示词 | 通过 | 扫描 `resources/app`，0 个 `*-source.png` / `*.prompt.txt`。 |
| 素材 | AI 内容分类与来源台账 | 通过 | `steam/ASSET_PROVENANCE.md` 已建立；发行方仍须在 Steam Survey 前确认所有可见素材的商业使用权。 |
| 素材 | 攻击动画质检中间产物不进包 | 通过 | 最终 `app.asar` 中 `*-source.png`、`.prompt.txt` 和 `assets/animations/attacks/*-qc/` 均为 0 个条目。 |
| 首局 | 加载、进入第 1 关 | 通过 | 打包资源自动化流程及截图。 |
| 首局 | 开始、暂停、恢复 | 通过 | 打包资源自动化流程。 |
| 生命周期 | 失焦后恢复战斗 | 通过 | 打包资源自动化流程。 |
| 生命周期 | 最小化后恢复 | 待测 | S03 实机窗口测试。 |
| 输入 | 鼠标建造、选择、升级、出售 | 通过 | 打包资源自动流程覆盖选择、开始与暂停；其余鼠标操作由既有 Canvas Pointer Events 实现，待长局手工验收复查。 |
| 输入 | 键盘暂停、提前开波、全屏 | 通过 | 解包资源 DOM 用例验证 `P`、空格、`N`；`F` 复用已验证的全屏实现。 |
| 显示 | 1280x800 桌面布局 | 通过 | `release-battle-1280x800.png`。 |
| 显示 | 16:9、16:10、超宽屏、低分辨率 | 部分通过 | `960×640` 与 `1366×768` 已截图验证；超宽屏和 16:10 保留至外部硬件验收。 |
| 存档 | 首次写入和下次读取 | 通过 | `save-store.cjs` 直接回归：两次写入后读取最新主文件。 |
| 存档 | 写入中断后从备份恢复 | 通过 | 原子替换前保留有效备份；回归覆盖损坏主文件读取 `.bak`。 |
| 存档 | 损坏存档不阻断结算 | 通过 | 无效 JSON 写入被拒绝且保留现有有效文件；前端已有“存档暂未更新”状态提示。 |
| 存档 | 云端冲突与覆盖规则 | 阻塞 | 需要 S07 的真实 Steam Cloud 接入。 |
| 战斗 | 五张地图、胜利与失败结算 | 通过 | 解包资源功能审计覆盖五图、双路、15 波、小首领、终局首领、胜利、失败与返回选关。 |
| 战斗 | 无尽 / 试炼模式边界 | 通过 | 满足无尽解锁前置后进入无尽普通难度；试炼模式进入并生成有效规则。 |
| 性能 | 常规局帧时间与内存基线 | 部分通过 | 可见窗口两次 p95 为 24.3 / 24.2 ms，达到常规 25 ms 目标；ToDesk 虚拟显示适配器有 GPU IPC 警告，需物理显示器复测。详见 `steam/PERFORMANCE.md`。 |
| 性能 | 敌人、投射物、特效压力局 | 未通过（当前环境） | 可见 14 塔 / 60 敌人两次 p95 为 60.6 / 66.7 ms，超过 33 ms 目标；对象上限仍生效。远程虚拟显示环境不适合作为最低配置结论，需物理显示器复测。 |
| Steamworks | 成就真实解锁与离线回补 | 阻塞 | 当前仅有本地 Electron IPC，尚无 Steamworks SDK / App ID。 |
| Steamworks | Steam Overlay | 阻塞 | 同上。 |
| 发行 | NSIS 安装 | 通过 | S01 静默安装并启动已验证。 |
| 发行 | NSIS 升级、卸载 | 待测 | 需要后续版本号递增后执行升级路径测试。 |
| 商店 | 文案只承诺已实现功能 | 通过 | `steam/store/STORE_COPY_DRAFT.md` 明确排除未验收的 Steamworks 功能及未决定的商业资料。 |
| 商店 | 五种实机镜头的候选资源渲染 | 通过（QA） | S08 候选资源在洞窟、羁绊、双路、首领、后期阵容五种测试战局中无控制台错误，均输出 `1920×1080` PNG；测试状态截图不可上传。 |
| 商店 | 无文字胶囊构图草稿 | 通过（内部审阅） | 横版 `1672×941` 和竖版 `1024×1536` 草稿已按 `CAPSULE_BRIEF.md` 归档并记录提示词；均非 Steam 成品，禁止上传。 |
| 商店 | 胶囊图、五张 1920×1080 实机截图 | 待测 | 截图清单见 `steam/store/SCREENSHOT_SHOTLIST.md`；待从最终候选包拍摄，不使用概念图。 |
| SteamPipe | VDF 具体配置本地校验 | 通过 | 用虚拟数字 ID、隔离输出与已验收 `dist/win-unpacked` 完成结构校验；未调用 SteamCMD。 |
| SteamPipe | depot 上传、测试分支下载 | 阻塞 | 需要 Steamworks 后台权限、真实 App ID/DepotID、SteamCMD 和该次上传授权。 |
| SteamPipe | VDF 模板不含凭据且不能自动公开 | 通过 | `npm run check:steampipe` 验证占位模板、空 `setlive` 与无凭据规则；无 AppID 时不能执行真实上传。 |
